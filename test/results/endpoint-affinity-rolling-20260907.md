# Endpoint 绑定、滚动重启与 TUI 验收

实施前基线：`c6667589359eb7d10af3f5fc256738d65ae4635d`。
使用仓库虚拟环境、临时配置和本地假上游，未调用付费模型。

## 基线

- 完整回归：142/142 通过。
- 单元/拆分进程测试：57/58 通过。失败项期待重放超过 24 小时的遥测；已将该测试迁移到现行保留期语义。
- 初次在隔离副本运行时缺少被 gitignore 排除的 policy；补齐本地配置后重新完成完整回归。

## 实现验收

- 完整回归：133/133 通过。旧 deployment 永久绑定、删除密文和可关闭绑定的测试已替换为固定 endpoint 语义。
- 单元、集成及终端测试：93/93 通过。
- 8 个独立进程并发创建同一 family，只产生一个获胜 endpoint。
- 24 个并发首请求、父子模型、缺失标识、旧密文无绑定、上游密文拒绝、429/500 重试、资源身份变化、数据库锁故障均有覆盖。
- 验证默认 48 小时续期、过期边界、描述去重、缓存淘汰、受限文件权限、在途活动续期和 24 小时日志清理隔离。
- 正常滚动切换与 worker 崩溃后恢复绑定；新 worker 启动失败时旧 worker 保持接流；双 worker 的遥测互不清除。
- SSE 含 60 个增量事件，滚动切换后完整收到终止事件。代表性测量：预热 0.580 秒，接流切换 0.00135 秒；切换期间 383 次健康请求全部成功，新请求失败数 0。
- 实际管理命令 start / restart / stop 通过。开启受管日志时，预热 0.553 秒、切换 0.00169 秒。
- 2,000→400 首次历史无 gap；首次空流后突发、覆盖、限额、保留期清理、旧快照和流重建按协议处理。
- gap 30 秒自动隐藏、`c` 确认、`r` 仅刷新、累计数保留、旧协议未知数量均有覆盖。
- 真实 PTY 验证 60/80/120/180 列下无按键自动刷新、正常退出和终端设置恢复；渲染测试确认状态、告警和页脚可见。
- `py_compile` 和 `git diff --check` 通过。

## 迁移与定时任务

当前本地配置已更新为 172800 秒；运行中的旧进程仍使用启动时的旧配置。
首次旧版迁移不能恢复内存绑定。旧会话结束后，先升级 routing，再排空并启动新版 serving。
后续 serving 重启使用持久化恢复与滚动接流。回退不能恢复删除密文的行为。

`tools/scheduled_restart.py` 默认在下一次北京时间 01:00 执行一次；支持 `--dry-run`、`--daily`、外部调度用 `--now`。
首次迁移必须显式传入 `--legacy-migration`。等待排空默认最多 300 秒，超时返回失败并保留进程。

以上切换耗时来自本地假上游。线上认证预热耗时由真实 Azure CLI 和凭据状态决定，首次旧版迁移不承诺无损恢复旧会话绑定。

## 首次线上切换记录（北京时间 2026-09-08）

01:00 定时任务已先升级 routing 并排空旧 serving。任务精简环境缺少 `HOME`，
本机 Azure CLI 包装脚本因此无法获取认证，首次 worker 启动失败。
随后使用正常登录环境恢复 serving，新 worker 于 01:01:53 启动，预热约 1.05 秒。
健康接口确认 serving/routing 正常、绑定模式为 endpoint、TTL 为 172800 秒。
新版启动后的事件未出现删除密文，转发实现也已移除删除密文及 taint 路径。

定时脚本现为子进程补齐账号真实家目录，并增加缺少 `HOME` 的回归测试。
此次失败说明静态凭据测试未覆盖本机 Azure CLI 包装脚本的环境要求。

## TUI 后续验收

事件类型和说明按新版语义中文化，等级保持 `DEBUG / INFO / WARN / ERROR`。
重试区分原地、同源换部署、换源；限流响应和降低权重分别展示。
统计记录、管理进程、接流进程和会话绑定状态按指定中文文案展示。
旧版删除密文事件未增加专用翻译逻辑。

默认 `./tui.sh` 已允许 serving 离线时启动。本机模式独立读取 routing 心跳和进程信息，
HTTP 恢复后自动回到在线取数。真实 PTY 测试覆盖默认脚本离线启动及恢复；
新增调度环境回归验证子进程得到账号真实 `HOME`。

后续完整单元/集成/终端测试为 102/102 通过；四行中文文案的额外测试在
60/80/120/180 列均通过；相关看板回归 4/4 通过。
